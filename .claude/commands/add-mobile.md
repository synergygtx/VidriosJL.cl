# /add-mobile — PWA + Push Notifications

Convierte tu app en PWA instalable con push notifications.
Service worker manual (no next-pwa/Serwist/Workbox), iOS Safari compatible,
14 gotchas documentados de produccion.

Pre-requisito: `/add-login` debe estar ejecutado.

## Ejecucion

Leer y ejecutar `.claude/skills/add-mobile/SKILL.md`.
